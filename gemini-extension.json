{
  "name": "primamcp",
  "description": "Verified, citable German & EU law for any LLM. Daily updates from official sources, hosted in Germany.",
  "version": "1.0.0",
  "settings": [
    {
      "name": "API Key",
      "description": "PRIMAMCP API key (sk-legal-…). Create one at https://mcp.planitprima.com (5-day free trial, no credit card).",
      "envVar": "PRIMAMCP_API_KEY"
    }
  ],
  "mcpServers": {
    "primamcp": {
      "command": "npx",
      "args": ["-y", "@planit-tech/primamcp"],
      "env": {
        "PRIMAMCP_API_KEY": "${PRIMAMCP_API_KEY}"
      }
    }
  }
}
